# 🚀 Frontend Deployment на Render

Руководство по развертыванию TravelHub Frontend на Render.com

---

## 📋 Что нужно перед началом

✅ Backend уже развёрнут на Render
✅ Backend URL: `https://travelhub-backend-xxxx.onrender.com`
✅ PostgreSQL база данных работает

---

## 🔧 Способ 1: Автоматический деплой (Blueprint)

### Если вы используете render.yaml

Render Blueprint (`render.yaml`) уже настроен для автоматического создания frontend сервиса.

**Что делает Blueprint:**
- Создаёт Web Service `travelhub-frontend`
- Использует Docker для сборки React приложения
- Автоматически деплоит при изменениях в `travelhub-ultimate/frontend/**`

### ⚠️ Важно: Настройка VITE_API_BASE_URL

После создания сервиса через Blueprint:

1. Откройте **Render Dashboard** → **travelhub-frontend** service
2. Перейдите в **Environment**
3. Добавьте переменную окружения:

```
Key: VITE_API_BASE_URL
Value: https://travelhub-backend-xxxx.onrender.com/api
```

**Замените** `travelhub-backend-xxxx` на **реальный URL вашего backend**!

4. **Важно:** После добавления переменной, нужно **Manual Deploy**:
   - Settings → Manual Deploy → Deploy latest commit

Это пересоберёт приложение с правильным API URL.

---

## 🔧 Способ 2: Ручное создание Web Service

### Шаг 1: Создайте новый Web Service

1. Откройте **Render Dashboard**: https://dashboard.render.com
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Нажмите **Connect**

### Шаг 2: Настройте сервис

**Name:**
```
travelhub-frontend
```

**Region:**
```
Oregon (US West)
```

**Branch:**
```
main
```
(или `claude/review-travel-agency-9A4Ks` если деплоите из feature branch)

**Root Directory:**
```
travelhub-ultimate/frontend
```

**Runtime:**
```
Docker
```

**Dockerfile Path:**
```
./Dockerfile
```

**Docker Command:** (оставьте пустым, используется CMD из Dockerfile)

**Plan:**
```
Free
```

### Шаг 3: Advanced Settings

Нажмите **Advanced** для дополнительных настроек:

**Auto-Deploy:**
```
Yes
```

**Build Command:** (оставьте пустым - Docker сам соберёт)

**Start Command:** (оставьте пустым - используется CMD из Dockerfile)

### Шаг 4: Environment Variables

⚠️ **КРИТИЧНО:** Добавьте переменные окружения:

#### Переменная 1: NODE_ENV
```
Key: NODE_ENV
Value: production
```

#### Переменная 2: VITE_API_BASE_URL
```
Key: VITE_API_BASE_URL
Value: https://travelhub-backend-xxxx.onrender.com/api
```

**Важно:** Замените `travelhub-backend-xxxx` на ваш реальный backend URL!

Как узнать backend URL:
1. Dashboard → travelhub-backend service
2. Скопируйте URL сверху страницы (например: `https://travelhub-backend-abc123.onrender.com`)
3. Добавьте `/api` в конец

### Шаг 5: Create Web Service

Нажмите **Create Web Service** внизу страницы.

Render начнёт:
1. ✅ Клонирование репозитория
2. ✅ Docker build (3-5 минут)
3. ✅ Развёртывание контейнера
4. ✅ Запуск сервера

---

## 📊 Проверка деплоя

### 1. Проверьте логи

В Web Service → **Logs** должны увидеть:

```
Building Dockerfile...
[builder] Successfully built abc123
[builder] Successfully tagged xyz789
Pushing image to registry...
Upload succeeded
==> Deploying...

> serve -s dist -l 3000

INFO: Accepting connections at http://localhost:3000
```

### 2. Откройте приложение

Render даст вам URL типа:
```
https://travelhub-frontend-xxxx.onrender.com
```

Откройте в браузере - должна загрузиться TravelHub приложение.

### 3. Проверьте подключение к API

1. Откройте **DevTools** (F12)
2. Перейдите на **Network** tab
3. Попробуйте выполнить действие (например, поиск отелей)
4. Убедитесь что запросы идут на `https://travelhub-backend-xxxx.onrender.com/api`

**Хорошие запросы:**
```
GET https://travelhub-backend-xxxx.onrender.com/api/hotels
Status: 200 OK
```

**Плохие запросы (ошибка):**
```
GET http://localhost:3000/api/hotels
Status: ERR_CONNECTION_REFUSED
```

Если видите localhost → **VITE_API_BASE_URL не установлен** или установлен неправильно.

---

## 🔄 Обновление API URL после деплоя

Если вы забыли установить `VITE_API_BASE_URL` или установили неправильно:

1. Dashboard → travelhub-frontend → **Environment**
2. Найдите `VITE_API_BASE_URL`
3. Нажмите **Edit** → измените значение
4. **Save**
5. ⚠️ **ВАЖНО:** Перейдите в **Settings** → **Manual Deploy** → **Deploy latest commit**

**Почему нужен Manual Deploy?**
`VITE_API_BASE_URL` используется **во время сборки** (build time), не runtime.
Просто изменение env var не пересобирает приложение - нужен новый деплой!

---

## 🔧 Обновление Backend URL в файле

### Способ 1: Через .env файл (не работает на Render)

`.env` файлы не деплоятся (они в `.dockerignore`).
Используйте **Environment Variables** в Render Dashboard!

### Способ 2: Environment Variables (правильно)

Всегда используйте Render Dashboard → Environment для установки:
- `VITE_API_BASE_URL`
- Любых других переменных окружения

---

## 🆘 Troubleshooting

### Проблема: "Failed to fetch" или "Network Error"

**Причина:** Frontend не может подключиться к backend.

**Решение:**
1. Проверьте `VITE_API_BASE_URL` в Environment Variables
2. Убедитесь что backend URL правильный и заканчивается на `/api`
3. Проверьте что backend работает (откройте `https://backend-url/health`)
4. После изменения VITE_API_BASE_URL → Manual Deploy

### Проблема: "API URL is localhost"

**Причина:** `VITE_API_BASE_URL` не установлен - используется значение по умолчанию.

**Решение:**
1. Environment → Add Environment Variable
2. Key: `VITE_API_BASE_URL`
3. Value: `https://travelhub-backend-xxxx.onrender.com/api`
4. Settings → Manual Deploy → Deploy latest commit

### Проблема: "CORS Error"

**Причина:** Backend не разрешает запросы с frontend домена.

**Решение:**
1. Откройте backend service
2. Environment → найдите `FRONTEND_URL`
3. Убедитесь что значение = `https://travelhub-frontend-xxxx.onrender.com`
4. Если нет - добавьте/измените и сохраните
5. Backend автоматически перезапустится

### Проблема: Build fails с "out of memory"

**Причина:** Free plan имеет лимит памяти.

**Решение:**
1. Проверьте `vite.config.ts` - может быть слишком много manual chunks
2. Упростите build configuration
3. Или используйте Starter plan ($7/month) с большей памятью

### Проблема: "Application failed to respond"

**Причина:** Сервер не слушает на правильном порту.

**Решение:**
Dockerfile уже настроен правильно:
```dockerfile
CMD ["serve", "-s", "dist", "-l", "3000"]
```

Render автоматически пробросит порт 3000.

---

## ✅ Checklist финальной проверки

Перед тем как считать деплой завершённым:

- [ ] Frontend service создан на Render
- [ ] `VITE_API_BASE_URL` установлен в Environment Variables
- [ ] Backend URL правильный (https://... и заканчивается на /api)
- [ ] Выполнен Manual Deploy после добавления env vars
- [ ] Build прошёл успешно (логи показывают "Upload succeeded")
- [ ] Frontend открывается в браузере
- [ ] DevTools → Network показывает запросы к правильному backend URL
- [ ] Backend `FRONTEND_URL` установлен на frontend URL
- [ ] Нет CORS ошибок в консоли браузера
- [ ] Основные функции работают (поиск, регистрация, логин)

---

## 🎯 Итоговая архитектура

После успешного деплоя:

```
┌─────────────────────────────────────────────────┐
│  User Browser                                    │
│  https://travelhub-frontend-xxxx.onrender.com  │
└────────────────────┬────────────────────────────┘
                     │
                     │ HTTP Requests
                     ▼
┌─────────────────────────────────────────────────┐
│  TravelHub Frontend (Render Web Service)        │
│  - React + Vite                                  │
│  - Static files served by 'serve'               │
│  - VITE_API_BASE_URL configured                 │
└────────────────────┬────────────────────────────┘
                     │
                     │ API Calls
                     ▼
┌─────────────────────────────────────────────────┐
│  TravelHub Backend (Render Web Service)         │
│  - Express + TypeScript                          │
│  - https://travelhub-backend-xxxx.onrender.com │
└────────────────────┬────────────────────────────┘
                     │
                     │ Database Queries
                     ▼
┌─────────────────────────────────────────────────┐
│  PostgreSQL Database (Render)                   │
│  - travelhub-db                                  │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Следующие шаги

После успешного деплоя frontend:

1. **Тестирование:**
   - Протестируйте все основные функции
   - Проверьте регистрацию/логин
   - Попробуйте поиск отелей/авиабилетов

2. **Custom Domain (опционально):**
   - Settings → Custom Domain
   - Добавьте свой домен (например: travelhub.com)
   - Настройте DNS

3. **SSL Certificate:**
   - Render автоматически выдаёт SSL сертификат
   - Проверьте что сайт открывается по HTTPS

4. **Monitoring:**
   - Настройте alerts для downtime
   - Следите за логами
   - Проверяйте Metrics

---

## 💡 Полезные ссылки

- **Render Dashboard:** https://dashboard.render.com
- **Render Docs:** https://render.com/docs
- **Render Docker Guide:** https://render.com/docs/docker
- **Render Environment Variables:** https://render.com/docs/environment-variables

---

**Готово!** Ваш TravelHub Frontend развёрнут и работает! 🎉

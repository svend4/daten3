# 🚀 Настройка PostgreSQL на Railway - Пошаговая инструкция

## Текущий статус
✅ **Код готов на 100%** - Все 41 эндпоинт переведены на Prisma ORM
⏳ **Требуется** - Настройка PostgreSQL базы данных на Railway

---

## 📋 Шаг 1: Создание PostgreSQL на Railway

### 1.1 Открыть Railway Dashboard
```
https://railway.app/
```

### 1.2 Выбрать проект
- Перейти в проект **daten3**
- Должны быть видны сервисы:
  - `daten3-travelbackend` (Backend)
  - `daten3-travelfrontend` (Frontend)

### 1.3 Добавить PostgreSQL
1. Нажать **"+ New"** в правом верхнем углу
2. Выбрать **"Database"**
3. Выбрать **"Add PostgreSQL"**
4. Подождать создания (~30 секунд)

### 1.4 Проверка создания
После создания появится новый сервис **"Postgres"**

---

## 📋 Шаг 2: Получение DATABASE_URL

### 2.1 Открыть настройки PostgreSQL
1. Кликнуть на сервис **Postgres**
2. Перейти на вкладку **"Variables"**

### 2.2 Скопировать DATABASE_URL
1. Найти переменную `DATABASE_URL`
2. Нажать на иконку **копирования** справа
3. URL будет выглядеть примерно так:
```
postgresql://postgres:PASSWORD@containers-us-west-XXX.railway.app:5432/railway
```

### 2.3 Сохранить для следующего шага
URL скопирован в буфер обмена ✅

---

## 📋 Шаг 3: Подключение к Backend

### 3.1 Открыть Backend сервис
1. Вернуться к списку сервисов (кнопка **Back** или стрелка назад)
2. Кликнуть на **daten3-travelbackend**

### 3.2 Добавить переменную окружения
1. Перейти на вкладку **"Variables"**
2. Нажать **"+ New Variable"**
3. Заполнить:
   - **Variable Name**: `DATABASE_URL`
   - **Value**: вставить скопированный URL из шага 2.2
4. Нажать **"Add"**

### 3.3 Автоматический редеплой
Railway автоматически запустит новый деплой после добавления переменной!

---

## 📋 Шаг 4: Проверка успешного деплоя

### 4.1 Открыть логи деплоя
1. Остаться в сервисе **daten3-travelbackend**
2. Перейти на вкладку **"Deployments"**
3. Кликнуть на самый верхний деплой (последний)
4. Нажать **"View Logs"**

### 4.2 Что искать в логах

#### ✅ Успешная генерация Prisma Client:
```
✔ Generated Prisma Client (v5.22.0) to ./node_modules/@prisma/client
```

#### ✅ Успешная компиляция TypeScript:
```
> travelhub-backend@1.0.0 build
> npx prisma generate && tsc

[без ошибок]
```

#### ✅ Миграции применены (если есть):
```
Prisma schema loaded from prisma/schema.prisma
Running migrations...
```

#### ✅ Сервер запущен:
```
🚀 Server is running on port XXXX
```

### 4.3 Если деплой упал
Проверить ошибки в логах и вернуться к шагу 3.2 - убедиться, что DATABASE_URL добавлен правильно.

---

## 📋 Шаг 5: Применение миграций (если требуется)

### 5.1 Проверка необходимости миграций
Если это первый деплой с Prisma, нужно создать таблицы в БД.

### 5.2 Запуск миграций вручную (опционально)

**Вариант A: Через Railway CLI** (если установлен)
```bash
railway link
railway run npx prisma migrate deploy
```

**Вариант B: Локально с DATABASE_URL**
```bash
# В папке backend создать .env
echo "DATABASE_URL=postgresql://..." > .env

# Запустить миграции
npx prisma migrate dev --name init

# Или только применить
npx prisma migrate deploy
```

**Вариант C: Автоматически при деплое**
Railway автоматически выполнит `npx prisma generate` из build script.
Для миграций нужно добавить в `nixpacks.toml`:

```toml
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma migrate deploy", "npm run build"]

[start]
cmd = "npm start"
```

### 5.3 Рекомендуемый подход
Добавить миграции в `nixpacks.toml` (уже должно быть настроено).

---

## 📋 Шаг 6: Проверка базы данных

### 6.1 Открыть PostgreSQL Dashboard
1. Вернуться к сервису **Postgres**
2. Перейти на вкладку **"Data"** (если доступна)

### 6.2 Проверка таблиц
Должны быть созданы следующие таблицы:
- ✅ `users`
- ✅ `bookings`
- ✅ `favorites`
- ✅ `price_alerts`
- ✅ `affiliates`
- ✅ `referrals`
- ✅ `commissions`
- ✅ `payouts`
- ✅ `affiliate_clicks`
- ✅ `refresh_tokens`
- ✅ `password_reset_tokens`

**Всего: 12 таблиц**

### 6.3 Альтернатива: Prisma Studio локально
```bash
cd backend
echo "DATABASE_URL=<твой_railway_url>" > .env
npx prisma studio
```
Откроется GUI на http://localhost:5555

---

## 📋 Шаг 7: Тестирование API

### 7.1 URL бэкенда
```
https://daten3-travelbackend.up.railway.app
```

### 7.2 Health Check
```bash
curl https://daten3-travelbackend.up.railway.app/health
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "TravelHub Backend is running",
  "timestamp": "2025-12-20T12:00:00.000Z"
}
```

### 7.3 Регистрация нового пользователя
```bash
curl -X POST https://daten3-travelbackend.up.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!",
    "firstName": "Тест",
    "lastName": "Пользователь"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "uuid...",
      "email": "test@example.com",
      "firstName": "Тест",
      "lastName": "Пользователь",
      "role": "user"
    },
    "accessToken": "eyJhbG...",
    "refreshToken": "eyJhbG..."
  }
}
```

### 7.4 Вход
```bash
curl -X POST https://daten3-travelbackend.up.railway.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!"
  }'
```

**Сохранить токен из ответа!**

### 7.5 Создание бронирования
```bash
# Замените YOUR_TOKEN на accessToken из предыдущего шага
curl -X POST https://daten3-travelbackend.up.railway.app/api/bookings \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "hotel",
    "itemId": "hotel_123",
    "itemName": "Grand Hotel Moscow",
    "checkIn": "2025-06-01",
    "checkOut": "2025-06-05",
    "guests": 2,
    "rooms": 1,
    "totalPrice": 25000,
    "currency": "RUB"
  }'
```

### 7.6 Получение списка бронирований
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://daten3-travelbackend.up.railway.app/api/bookings
```

### 7.7 Добавление в избранное
```bash
curl -X POST https://daten3-travelbackend.up.railway.app/api/favorites \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "hotel",
    "itemId": "hotel_456",
    "name": "Hilton Moscow",
    "location": "Москва, Россия",
    "price": 5000,
    "currency": "RUB",
    "rating": 4.5
  }'
```

---

## 📋 Шаг 8: Заполнение тестовыми данными (опционально)

### 8.1 Seed данные локально
```bash
cd backend
echo "DATABASE_URL=<railway_url>" > .env
npm run prisma:seed
```

### 8.2 Тестовые учетные записи после seed

#### Обычный пользователь:
```
Email: user@travelhub.com
Password: Test123!
```

#### Администратор:
```
Email: admin@travelhub.com
Password: Test123!
```

#### Партнер:
```
Email: affiliate@travelhub.com
Password: Test123!
Referral Code: REF001TRAVELHUB
```

### 8.3 Тестирование с admin аккаунтом
```bash
# 1. Вход как admin
curl -X POST https://daten3-travelbackend.up.railway.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@travelhub.com",
    "password": "Test123!"
  }'

# Сохранить ADMIN_TOKEN из ответа

# 2. Получить всех партнеров
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  https://daten3-travelbackend.up.railway.app/api/admin/affiliates

# 3. Получить аналитику
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  https://daten3-travelbackend.up.railway.app/api/admin/analytics

# 4. Топ исполнителей
curl -H "Authorization: Bearer ADMIN_TOKEN" \
  https://daten3-travelbackend.up.railway.app/api/admin/analytics/top-performers
```

---

## 📋 Шаг 9: Подключение Frontend к новому Backend

### 9.1 Проверить переменные Frontend
1. Открыть сервис **daten3-travelfrontend**
2. Перейти в **Variables**
3. Убедиться, что есть:
```
VITE_API_URL=https://daten3-travelbackend.up.railway.app
```

### 9.2 Если переменной нет
Добавить:
- **Variable Name**: `VITE_API_URL`
- **Value**: `https://daten3-travelbackend.up.railway.app`

Frontend автоматически редеплоится ✅

---

## ✅ Чеклист финального завершения

### Backend
- [ ] PostgreSQL создан на Railway
- [ ] DATABASE_URL добавлен в Backend variables
- [ ] Деплой прошел успешно (проверить логи)
- [ ] Миграции применены (12 таблиц созданы)
- [ ] Health check возвращает успешный ответ
- [ ] Регистрация работает (создается пользователь в БД)
- [ ] Логин работает (выдается JWT токен)
- [ ] Создание бронирования работает
- [ ] Админ эндпоинты работают

### Frontend
- [ ] VITE_API_URL указывает на правильный backend
- [ ] Frontend открывается без ошибок
- [ ] Можно зарегистрироваться через UI
- [ ] Можно войти через UI
- [ ] Можно создать бронирование через UI

### Опционально
- [ ] Seed данные добавлены
- [ ] Протестированы тестовые аккаунты
- [ ] Prisma Studio подключен для просмотра БД

---

## 🎯 Следующие шаги после настройки БД

1. **Фаза 11**: RBAC - Система ролей и разграничение доступа
2. **Фаза 12**: Интеграция с реальными API (Booking.com, Aviasales)
3. **Фаза 13**: Уведомления (Email, Push)
4. **Фаза 14**: Админ панель (расширенная)
5. **Фаза 15**: Мониторинг и аналитика

---

## 🆘 Troubleshooting

### Проблема: "Prisma Client not generated"
**Решение:**
```bash
# В логах Railway должно быть:
npx prisma generate && tsc

# Если нет - проверить package.json:
"build": "npx prisma generate && tsc"
```

### Проблема: "Table does not exist"
**Решение:**
Миграции не применены. Варианты:
1. Добавить в `nixpacks.toml`: `npx prisma migrate deploy`
2. Запустить локально: `npx prisma migrate deploy`

### Проблема: "Connection timeout"
**Решение:**
Проверить DATABASE_URL - должен быть полный URL с паролем.

### Проблема: "JWT_SECRET not defined"
**Решение:**
Добавить в Backend Variables:
```
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-key-also-long
```

### Проблема: TypeScript ошибки при деплое
**Решение:**
Уже исправлено в последнем коммите! Если все еще есть - проверить, что запушены последние изменения:
```bash
git log --oneline -5
# Должен быть: 1585644 fix: Add missing fields to Prisma schema
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверить логи деплоя в Railway
2. Проверить переменные окружения
3. Попробовать локальный запуск с Railway DATABASE_URL
4. Использовать Prisma Studio для просмотра БД

---

**Удачи с настройкой! После завершения всех шагов база данных будет полностью работать с 41 эндпоинтом!** 🚀

# Frontend Issues Report - Проблемы с Интерфейсом

Дата: 2025-12-23
Статус: 🔴 Критические проблемы найдены

## 📊 Краткое Резюме

Обнаружено **7 критических проблем** с кнопками и функциональностью фронт-энда, которые мешают пользователям корректно взаимодействовать с приложением.

---

## 🚨 Критические Проблемы

### 1. **Нефункциональный виджет поиска на главной странице**
**Статус:** 🔴 Критическая
**Файлы:**
- `src/pages/Home.tsx` (строка 202)
- `src/components/features/SearchWidget.tsx`

**Проблема:**
На главной странице используется упрощенный `SearchWidget` компонент, который является заглушкой без реальной функциональности.

**Текущий код:**
```tsx
// src/components/features/SearchWidget.tsx (строки 31-35)
{activeTab === 'hotels' ? (
  <div>Форма поиска отелей</div>
) : (
  <div>Форма поиска авиабилетов</div>
)}
```

**Воздействие:**
- ❌ Пользователи НЕ могут искать отели
- ❌ Пользователи НЕ могут искать авиабилеты
- ❌ Главная функциональность приложения недоступна
- ❌ Кнопка поиска отсутствует полностью

**Решение:**
Заменить `SearchWidget` на `SearchWidgetExtended`, который содержит полноценные формы поиска с датами, количеством гостей и кнопкой поиска.

---

### 2. **Аутентификация всегда показывает "Не авторизован"**
**Статус:** 🔴 Критическая
**Файл:** `src/components/layout/Header.tsx` (строка 36)

**Проблема:**
В Header компоненте `isAuthenticated` жестко закодирован как `false`:

```typescript
const isAuthenticated = false; // TODO: Get from auth context
```

**Воздействие:**
- ❌ Пользователи всегда видят "Войти/Регистрация", даже после авторизации
- ❌ Кнопки профиля, избранного, уведомлений не отображаются
- ❌ Невозможно проверить работу авторизованных функций
- ❌ Плохой UX - пользователь не понимает свой статус

**Решение:**
Подключить AuthContext для получения реального статуса авторизации:

```typescript
import { useAuth } from '../../store/AuthContext';

export default function Header() {
  const { isAuthenticated } = useAuth();
  // ...
}
```

---

### 3. **"Coming Soon" заглушки в критических настройках**
**Статус:** 🟡 Важная
**Файл:** `src/pages/Settings.tsx` (строки 300, 312, 324)

**Проблема:**
В настройках есть 3 раздела с заглушками:

```tsx
<div className="text-center text-gray-500 py-8">
  Coming Soon
</div>
```

**Затронутые разделы:**
1. **Настройки уведомлений** - пользователи не могут управлять уведомлениями
2. **Настройки конфиденциальности** - нет контроля над приватностью
3. **Подключенные аккаунты** - нельзя подключить соц. сети

**Воздействие:**
- ⚠️ Снижена функциональность настроек
- ⚠️ Пользователи не могут настроить важные параметры
- ⚠️ Плохое впечатление от незавершенности

**Решение:**
Реализовать или временно скрыть эти разделы до их полной готовности.

---

### 4. **Отсутствует реализация API для бронирований**
**Статус:** 🟡 Важная
**Файл:** `src/hooks/useBookings.ts` (строка 9)

**Проблема:**
Hook для бронирований содержит только TODO:

```typescript
// TODO: Implement API call
```

**Воздействие:**
- ❌ Страница "Мои бронирования" не работает
- ❌ Пользователи не видят свои заказы
- ❌ Невозможно отменить или изменить бронирование

**Решение:**
Реализовать API вызовы к бэкенду `/api/bookings`.

---

### 5. **Кнопка "Аренда авто" ведет на несуществующую страницу**
**Статус:** 🟡 Важная
**Файлы:**
- `src/pages/Home.tsx` (строка 191-199)
- `src/components/features/SearchWidgetExtended.tsx` (строка 286-291)

**Проблема:**
1. Кнопка "🚗 Аренда авто" в виджете поиска активна
2. Но показывает только "Car rental search coming soon..."
3. Ссылки в навигации ведут на `/cars`, которая не существует

**Воздействие:**
- ⚠️ Обманутые ожидания пользователей
- ⚠️ 404 ошибка при клике на навигацию
- ⚠️ Неработающий функционал

**Решение:**
Либо реализовать поиск аренды авто, либо временно скрыть эту опцию.

---

### 6. **Ссылки на несуществующие маршруты в навигации**
**Статус:** 🟠 Средняя
**Файл:** `src/components/layout/Header.tsx` (строки 39-43)

**Проблемы:**
```typescript
{ name: 'Отели', href: '/hotels/search', icon: '🏨' },      // ✅ Существует
{ name: 'Авиабилеты', href: '/flights/search', icon: '✈️' }, // ❌ Должно быть /flights
{ name: 'Аренда авто', href: '/cars', icon: '🚗' },          // ❌ Не существует
{ name: 'Мои поездки', href: '/bookings', icon: '🎒' }      // ✅ Существует
```

**Воздействие:**
- ⚠️ 404 ошибки при навигации
- ⚠️ Плохой UX

**Решение:**
Исправить маршруты или скрыть неработающие ссылки.

---

### 7. **Популярные направления ведут на неправильный URL**
**Статус:** 🟠 Средняя
**Файл:** `src/pages/Home.tsx` (строка 249)

**Проблема:**
Карточки популярных направлений используют неправильный URL:

```tsx
to={`/hotels/search?destination=${destination.city}`}
```

**Но в App.tsx маршрут:**
```tsx
<Route path="/hotels" element={<HotelSearch />} />  // Без /search
```

**Воздействие:**
- ⚠️ Клик по популярным направлениям не работает
- ⚠️ 404 ошибка вместо поиска

**Решение:**
Изменить ссылку на `/hotels?destination=${destination.city}` или добавить маршрут `/hotels/search`.

---

## 📋 Полный Список Найденных Проблем

| # | Проблема | Приоритет | Файл | Статус |
|---|----------|-----------|------|--------|
| 1 | Нефункциональный SearchWidget | 🔴 Критическая | Home.tsx, SearchWidget.tsx | Не исправлено |
| 2 | isAuthenticated = false | 🔴 Критическая | Header.tsx:36 | Не исправлено |
| 3 | "Coming Soon" в Settings | 🟡 Важная | Settings.tsx | Не исправлено |
| 4 | TODO в useBookings | 🟡 Важная | useBookings.ts:9 | Не исправлено |
| 5 | Аренда авто не работает | 🟡 Важная | Home.tsx, SearchWidgetExtended.tsx | Не исправлено |
| 6 | Неверные маршруты в Header | 🟠 Средняя | Header.tsx | Не исправлено |
| 7 | Популярные направления 404 | 🟠 Средняя | Home.tsx:249 | Не исправлено |

---

## 🔧 План Исправления

### Этап 1: Критические (Обязательно)
1. ✅ Заменить SearchWidget на SearchWidgetExtended на главной
2. ✅ Подключить AuthContext в Header
3. ✅ Исправить маршруты в навигации

### Этап 2: Важные (Рекомендуется)
4. Реализовать useBookings API calls
5. Скрыть "Coming Soon" разделы в Settings
6. Отключить или реализовать Аренду авто

### Этап 3: Улучшения (Опционально)
7. Исправить ссылки популярных направлений
8. Добавить валидацию форм
9. Улучшить обработку ошибок

---

## 📝 Рекомендации

1. **Немедленно** исправить критические проблемы 1-2
2. **В ближайшее время** решить проблемы 3-5
3. **Скрыть незавершенные функции** вместо показа "Coming Soon"
4. **Добавить E2E тесты** для проверки навигации и форм
5. **Документировать** все незавершенные функции в отдельном TODO.md

---

## ✅ Следующие Шаги

1. Создать branch для исправлений: `fix/frontend-buttons-issues`
2. Исправить критические проблемы (1-2)
3. Протестировать навигацию и формы
4. Создать PR с описанием исправлений
5. Deploy и проверка на production

---

*Отчет создан автоматически при аудите фронт-энда.*

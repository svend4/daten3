# TravelHub Production Environment Variables (HTTPS)
# Use this template for production deployment with SSL/HTTPS

# Node environment
NODE_ENV=production

# Ports (internal - Coolify/Nginx will handle external HTTPS)
FRONTEND_PORT=80
BACKEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379

# PostgreSQL
# IMPORTANT: Use strong passwords in production!
POSTGRES_DB=travelhub_prod
POSTGRES_USER=travelhub_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# JWT Secrets (generate with: openssl rand -base64 32)
# IMPORTANT: Generate unique secrets for production!
JWT_SECRET=GENERATE_WITH_openssl_rand_base64_32
JWT_REFRESH_SECRET=GENERATE_WITH_openssl_rand_base64_32

# Frontend URL (HTTPS in production)
FRONTEND_URL=https://travelhub.yourdomain.com

# Frontend API URL (HTTPS in production)
VITE_API_BASE_URL=https://api.travelhub.yourdomain.com/api

# CORS Configuration (for production)
CORS_ORIGIN=https://travelhub.yourdomain.com

# Optional: Additional security settings
# SESSION_SECRET=GENERATE_ANOTHER_SECRET
# COOKIE_DOMAIN=.yourdomain.com
# COOKIE_SECURE=true

# Optional: External Services (if using)
# TRAVELPAYOUTS_TOKEN=your_token_here
# TRAVELPAYOUTS_MARKER=your_marker_here
# REDIS_URL=redis://redis:6379
# DATABASE_URL=postgresql://user:password@db:5432/travelhub

# Optional: Email configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# Optional: Monitoring and logging
# SENTRY_DSN=https://your-sentry-dsn
# LOG_LEVEL=info
# ENABLE_METRICS=true

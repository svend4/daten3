# 🔗 Соединение Frontend ↔ Backend

## ✅ Текущий статус:
- Frontend: РАБОТАЕТ ✅
- Backend: РАБОТАЕТ (status OK) ✅
- Осталось: СОЕДИНИТЬ! ⏳

---

## 📋 Пошаговая инструкция соединения

### Шаг 1: Скопируйте Backend URL

В **Railway Dashboard**:
1. Откройте **Backend service**
2. Вкладка **Settings**
3. Раздел **Networking** → **Public Networking**
4. Скопируйте Domain URL

**Пример**: `https://daten3-travelbackend.up.railway.app`

---

### Шаг 2: Добавьте переменную во Frontend

В **Railway Dashboard**:
1. Откройте **Frontend service** (daten3-travel)
2. Вкладка **Variables**
3. Нажмите **+ New Variable**
4. Добавьте:

```
Variable: VITE_API_BASE_URL
Value:    https://daten3-travelbackend.up.railway.app/api
```

⚠️ **ВАЖНО**:
- URL должен заканчиваться на `/api`
- Без trailing slash в конце!
- Замените `daten3-travelbackend.up.railway.app` на ваш реальный Backend URL

**Пример правильного значения**:
```
https://daten3-travelbackend.up.railway.app/api
```

5. Нажмите **Add** (или **Save**)

---

### Шаг 3: Проверьте Backend переменные

В **Railway Dashboard**:
1. Откройте **Backend service**
2. Вкладка **Variables**
3. Убедитесь что есть:

```
FRONTEND_URL=https://daten3-travel.up.railway.app
NODE_ENV=production
```

⚠️ **ВАЖНО**:
- FRONTEND_URL должен совпадать с вашим Frontend URL
- БЕЗ trailing slash (не .app/)

Если переменных нет, добавьте их:

**Все Backend переменные**:
```
FRONTEND_URL=https://daten3-travel.up.railway.app
NODE_ENV=production
JWT_SECRET=7f9d8a6e5c4b3a2f1e0d9c8b7a6f5e4d3c2b1a0f9e8d7c6b5a4f3e2d1c0b9a8f7e6d
```

---

### Шаг 4: Дождитесь автоматического Redeploy

После добавления `VITE_API_BASE_URL` в Frontend:

1. **Railway автоматически** начнёт redeploy Frontend
2. В разделе **Deployments** увидите новый деплой
3. Статус изменится: Building → Deploying → Active
4. Это займёт **2-3 минуты**

**НЕ нужно ничего делать вручную!** Railway сам передеплоит.

---

### Шаг 5: Проверка интеграции

#### Способ 1: В браузере (2 минуты)

1. **Откройте Frontend**: https://daten3-travel.up.railway.app
2. **Нажмите F12** (DevTools)
3. **Вкладка Console**
4. **Вставьте код**:

```javascript
// Проверка 1: API URL установлен?
console.log('API URL:', import.meta.env.VITE_API_BASE_URL);

// Проверка 2: Backend отвечает?
fetch(import.meta.env.VITE_API_BASE_URL + '/health')
  .then(r => r.json())
  .then(d => console.log('✅ Backend подключён:', d))
  .catch(e => console.error('❌ Ошибка:', e));
```

**Ожидаемый результат**:
```
API URL: https://daten3-travelbackend.up.railway.app/api
✅ Backend подключён: {status: "ok", timestamp: "...", uptime: 123}
```

#### Способ 2: На мобильном (1 минута)

1. Откройте: https://daten3-travel.up.railway.app
2. Интерфейс должен работать
3. Попробуйте поискать отели или авиабилеты
4. В будущем здесь появятся данные от backend

---

## 🎯 Финальная проверка

### ✅ Всё работает если:

```bash
# Frontend
https://daten3-travel.up.railway.app
→ Страница загружается ✅

# Backend Health
https://daten3-travelbackend.up.railway.app/health
→ {"status":"ok",...} ✅

# В Frontend Console:
import.meta.env.VITE_API_BASE_URL
→ "https://daten3-travelbackend.up.railway.app/api" ✅

# Fetch test в Console:
fetch(...)
→ ✅ Backend подключён ✅
```

### ❌ Если есть проблемы:

**"VITE_API_BASE_URL is undefined"**:
- Переменная не добавлена или redeploy не завершён
- Подождите завершения redeploy
- Обновите страницу (Ctrl+F5)

**"CORS error"**:
- Проверьте FRONTEND_URL в Backend Variables
- Должен совпадать с реальным Frontend URL
- Без trailing slash

**"Failed to fetch"**:
- Backend не запущен
- Проверьте Backend service в Railway
- Должен быть зелёный статус "Active"

---

## 📊 Архитектура после соединения

```
┌─────────────────────────────────────────┐
│              User Browser               │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Frontend (daten3-travel.up.railway.app)│
│  ├─ React App                           │
│  ├─ VITE_API_BASE_URL → backend/api     │
│  └─ fetch(API_URL + '/hotels/search')   │
└──────────────────┬──────────────────────┘
                   │ HTTP Request
                   │ GET /api/hotels/search
                   ▼
┌─────────────────────────────────────────┐
│  Backend (daten3-travelbackend...)      │
│  ├─ Express :3000                       │
│  ├─ CORS: FRONTEND_URL ✅               │
│  ├─ GET /api/health                     │
│  ├─ GET /api/hotels/search              │
│  └─ GET /api/flights/search             │
└─────────────────────────────────────────┘
```

---

## 🚀 Быстрая команда для проверки

**Скопируйте в Console Frontend**:
```javascript
(async () => {
  const api = import.meta.env.VITE_API_BASE_URL;
  console.log('🔗 API URL:', api);

  if (!api) {
    console.error('❌ VITE_API_BASE_URL не установлен!');
    console.log('Добавьте переменную в Railway Frontend Variables');
    return;
  }

  try {
    const r = await fetch(api + '/health');
    const d = await r.json();
    console.log('✅ СОЕДИНЕНИЕ РАБОТАЕТ!');
    console.log('Backend:', d);
    console.log('Uptime:', Math.floor(d.uptime), 'секунд');
  } catch (e) {
    console.error('❌ Ошибка соединения:', e.message);
    if (e.message.includes('CORS')) {
      console.log('Проверьте FRONTEND_URL в Backend Variables');
    }
  }
})();
```

---

## ✅ Готово!

После выполнения всех шагов у вас будет:
- ✅ Frontend подключен к Backend
- ✅ API запросы работают
- ✅ CORS настроен
- ✅ Полная интеграция

**Время выполнения**: 5 минут (включая redeploy)

---

**Следующий шаг**: После соединения можно добавлять функциональность:
- Реальный поиск отелей
- Реальный поиск авиабилетов
- Бронирование
- Аутентификация пользователей

# ⚡ Быстрое исправление CORS (2 минуты)

## 🎯 Проблема
Frontend не может подключиться к Backend из-за CORS.

## ✅ Решение в 6 шагов

### 1️⃣ Откройте Render Dashboard
```
https://dashboard.render.com
```

### 2️⃣ Найдите backend service
- Название: `daten3-1` или `travelhub-backend`

### 3️⃣ Откройте Environment
- Нажмите **"Environment"** в левом меню

### 4️⃣ Добавьте переменную
```
Key:   FRONTEND_URL
Value: https://daten3.onrender.com
```

Нажмите **"Add Environment Variable"** или **"+"**

### 5️⃣ Сохраните
- Нажмите **"Save Changes"**
- Backend автоматически передеплоится (подождите 3-5 минут)

### 6️⃣ Проверьте
- Откройте: https://daten3.onrender.com
- Проверьте консоль браузера (F12)
- Не должно быть ошибок CORS

---

## 🧪 Быстрая проверка

Откройте консоль браузера на https://daten3.onrender.com и выполните:

```javascript
fetch('https://daten3-1.onrender.com/api/health', {
  credentials: 'include'
})
.then(r => r.json())
.then(d => console.log('✅ Backend доступен:', d))
.catch(e => console.error('❌ Ошибка:', e.message))
```

**Ожидаемый результат:**
```
✅ Backend доступен: {status: "ok", ...}
```

---

## ❌ Если не работает

Проверьте логи backend в Render Dashboard:
- Backend Service → Logs
- Ищите строку: `CORS Configuration initialized`
- Должно быть: `frontendUrl: https://daten3.onrender.com`

Если нет — переменная не применилась, попробуйте снова.

---

**Время: ~5 минут (включая передеплой)**
